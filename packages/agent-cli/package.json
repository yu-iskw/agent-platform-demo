{
  "name": "@agent-platform/agent-cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for calling the remote agent with a delegated Google access token",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "agent-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --import tsx src/index.ts"
  },
  "dependencies": {
    "@agent-platform/agent-client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.3"
  }
}
